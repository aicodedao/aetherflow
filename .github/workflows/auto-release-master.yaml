# .github/workflows/auto_release_final.yml
name: Auto Release (Final) - master branch via PR + tags

on:
  workflow_run:
    workflows: ["CI"]   # đổi đúng tên workflow CI của repo mày
    types: [completed]
    branches: [master]

permissions:
  contents: write
  pull-requests: write
  checks: read
  statuses: read

concurrency:
  group: auto-release-final-master
  cancel-in-progress: false

jobs:
  release_final:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout master
        uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0

      - name: Set git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Run release (Final) via PR
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python release.py --mode final --push --push-via-pr --skip-ci-check
